import { c as createPluginWithCommands, d as defaultPlugin } from './shared/eslint-plugin-command.32c929b6.mjs';
import './shared/eslint-plugin-command.b1b73995.mjs';
import '@es-joy/jsdoccomment';

function config(options = {}) {
  const plugin = options.commands ? createPluginWithCommands(options) : defaultPlugin;
  const {
    name = "command"
  } = options;
  return {
    name,
    plugins: {
      [name]: plugin
    },
    rules: {
      [`${name}/command`]: "error"
    }
  };
}

export { config as default };
